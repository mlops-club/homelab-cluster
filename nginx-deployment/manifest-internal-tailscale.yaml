# Apply this manifest with: kubectl apply -f nginx-deployment/manifest-tailscale.yaml
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-tailscale
  labels:
    app: nginx-tailscale
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-tailscale
  template:
    metadata:
      labels:
        app: nginx-tailscale
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-tailscale
  labels:
    app: nginx-tailscale
  annotations:
    tailscale.com/expose: "true"
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: nginx-tailscale
